// reduce.cpp
#include "reduce.h"

void reduce(std::unordered_map<std::string, int>& wordCounts) {
    // In this simple example, the map step already performs counting,
    // so the reduce step could be a no-op or could perform additional
    // transformation if needed.
}